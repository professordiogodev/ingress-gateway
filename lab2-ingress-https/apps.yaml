---
apiVersion: v1
kind: ConfigMap
metadata:
  name: webapp-html
  namespace: ${STUDENT_NAME}
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head><title>Secure WebApp</title></head>
    <body style="background:#6200ea;color:#fff;font-family:sans-serif;display:flex;align-items:center;justify-content:center;height:100vh;margin:0">
      <div style="text-align:center">
        <h1>🔒 Secure WebApp</h1>
        <p>Namespace: ${STUDENT_NAME}</p>
        <p>Lab 2 — Ingress HTTPS</p>
        <p>You are connected over TLS!</p>
      </div>
    </body>
    </html>
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
  namespace: ${STUDENT_NAME}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: html
          configMap:
            name: webapp-html
---
apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: ${STUDENT_NAME}
spec:
  selector:
    app: webapp
  ports:
    - port: 80
      targetPort: 80
